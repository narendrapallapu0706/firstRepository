firstRepository is a repository name
This project is for a  number guessing 
for suppose if we guess a number in between a numbers which is given in the code
  for example in between 1 to 100
if we guess a correct number which is given by system it shows us it is correct
  Otherwise,it tells less than the number or greater than the number 
User will give limited attempts to guess the correct number.
